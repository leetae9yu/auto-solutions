---
name: auto-solutions
description: "업로드된 수학 시험지 PDF·이미지를 원문 그대로 LaTeX로 전사하고 난이도별 단계 해설을 작성해, 풀이 공간이 있는 2단 문제지와 빠른정답부터 본문까지 전 페이지가 2단인 해설지를 각각 PDF·DOCX로 완성한다. 사용자가 문제지 업로드, 시험지 전사, 문제지·해설지 제작, LaTeX 시험지, 풀이 공간, 빠른정답, PDF/DOCX 산출물을 요청할 때 사용한다."
---

# 시험지·해설지 제작

최종 납품물은 아래 네 파일뿐이다.

- `<이름>_questions.pdf`
- `<이름>_questions.docx`
- `<이름>_solutions.pdf`
- `<이름>_solutions.docx`

합본과 LaTeX 소스는 작업 파일로만 사용하고 납품하지 않는다.

## 원칙

1. 원문을 고치거나 요약하지 않는다. 문구, 번호, 배점, 출전, 선지, 표,
   그래프, 도형, 조건 상자를 모두 보존한다.
2. 원본의 번호 누락·중복·오류는 임의 교정하지 말고 눈에 띄는 주석으로
   기록한다.
3. 문항별로 `원문 → LaTeX → PDF` 대응을 확인한다. 그림을 생략하거나
   텍스트 설명으로 대체하지 않는다.
4. 해설은 고등학교 교육과정 범위에서 작성한다. 정답만 제시하지 말고
   문항 난이도에 맞는 단계별 풀이로 사고 과정과 계산 근거를 보여 준다.
5. 문제지에는 답이나 해설을 넣지 않고, 각 문항 뒤에 실제 필기 가능한
   빈 풀이 공간을 둔다.
6. 문제지와 해설지는 아래의 고정 포맷을 따른다. 특히 해설지는
   빠른정답뿐 아니라 모든 해설 본문도 끝까지 2단으로 배치한다.

## 고정 포맷

- A4 세로, 문서 클래스 10pt, Noto Sans CJK KR, 상·하 13mm와 좌·우
  14mm 여백을 고정값으로 사용한다. 문제 원문은 `\footnotesize`,
  해설 레코드는 `\small`을 사용한다.
- 머리말 왼쪽에는 시험명, 오른쪽에는 자료명을 작게 넣고, 바닥글
  가운데에 페이지 번호를 둔다.
- 문제지와 해설지 모두 `multicols*` 2단, 단 사이 7mm, 0.35pt 세로
  구분선을 사용한다.
- 문항은 회색 배경 머리글에 `문항 <번호>`와 배점·출전을 표시한다.
  한 단 높이 안에 들어가는 문항은 전체를 분할 불가 블록으로 묶어
  단 또는 페이지 경계에서 쪼개지지 않게 한다.
- 문제지는 문항 아래에 회색 `풀이` 표시와 필기 공간을 둔다.
- 해설지는 첫 페이지에 테두리 없는 2단 빠른정답을 배치하고, 다음
  페이지부터 각 문항의 `원문 → 풀이 → 최종 답`을 같은 회색 문항
  머리글 아래에 수록한다.
- 해설 본문 전체를 하나의 `multicols*` 2단 흐름으로 유지한다.
  일부 문항만 임의로 1단으로 바꾸거나 빠른정답 뒤에서 2단을
  종료하지 않는다.
- 원본 번호 누락·중복과 특이 순서는 노란 안내 상자로 표시한다.
- 위 수치와 시각 체계를 임의로 재설계하지 않는다. 해설 하나가
  그림을 포함해 한 단의 사용 가능 높이보다 길 때만 번호가 붙은
  단계 사이에서 나누고, 다음 단에 같은 회색 머리글을
  `문항 <번호> (계속)`으로 반복한다. 한 단계나 그림 내부는 나누지
  않으며, 1단 전환·글자 축소·여백 변경으로 맞추지 않는다.
- 문제지에서 원문과 최소 풀이 공간의 합이 한 단보다 길면 원문
  블록은 그대로 두고 풀이 공간만 다음 단의 `풀이 (계속)`으로
  이어 간다. 원문 자체가 한 단보다 긴 예외에는 문단·조건·선지
  사이에서만 나누고 다음 단에 `문항 <번호> (계속)` 머리글을
  반복한다. 표·그림·조건 상자 내부는 나누지 않는다.

## 작업 순서

### 입력 확인

- 첨부를 언급하면 `/home/opc/oracle-shared`에서 가장 최근 원본을 찾는다.
- PDF 페이지 수와 이미지 해상도를 확인하고 모든 페이지를 렌더링한다.
- 물리 문항 수와 인쇄된 번호 순서를 별도로 기록한다.

### 원문 전사

- XeLaTeX용 `fontspec`, `xeCJK`, `amsmath`, `tikz`, `booktabs`,
  `multicol`을 사용한다.
- 한글 어절 내부 줄바꿈을 막도록
  `\xeCJKsetup{CJKspace=true,CJKglue={\nobreak\hskip 0pt}}`를 적용한다.
- 표는 `tabular`, 그래프·도형은 TikZ로 재작성한다. 복잡한 그림만
  원본에서 무손실 크롭하여 삽입한다.
- 문항은 재사용 가능한 레코드 매크로 하나로 관리하여 문제지와
  해설지가 동일 원문을 공유하게 한다.

### 문제지

- 시험지처럼 2단으로 배치하되 한 문항이 단 경계에서 잘리지 않게 한다.
- 각 문항 끝에 `풀이` 표시와 최소 18mm 빈 공간을 둔다.
- 긴 서술형·그래프 문항은 28~40mm 또는 다음 단 전체를 풀이 공간으로
  확보한다. 문항을 줄여 공간을 만들지 않는다.

### 해설지

- 첫 페이지에 빠른정답을 2단 단순 목록으로 배치한다. 테이블과 셀
  테두리를 쓰지 않는다. 왼쪽 단을 위에서 아래로 채운 뒤 오른쪽
  단으로 자연스럽게 흐르게 한다.
- 이후 문항 번호 순서대로 원문, 단계별 풀이, 최종 답을 수록한다.
- 각 문항의 풀이 난이도를 먼저 내부적으로 `기본`, `보통`, `심화`로
  판정한다. 난이도 표시는 원문에 없는 경우 문서에 새로 인쇄하지
  않는다.
  - `기본`: 핵심 개념 확인, 식 세우기·계산, 답 확인의 2~3단계로
    짧고 직접적으로 설명한다.
  - `보통`: 조건 해석, 전략 선택, 식 전개, 계산, 답 확인의 3~5단계로
    설명한다.
  - `심화`: 여러 조건의 연결, 경우 분류 또는 보조 결과, 본 계산,
    검산·조건 확인을 포함해 5단계 이상으로 충분히 설명한다.
- 단계는 `\textbf{1단계.}`, `\textbf{2단계.}`처럼 눈에 보이게
  구분한다. 단순 문항을 억지로 늘리거나, 심화 문항의 핵심 추론을
  한 문장으로 건너뛰지 않는다.
- 식 변형마다 사용한 정의·공식·조건을 바로 앞뒤에서 설명하고,
  마지막 단계에서 구한 값이 문제의 요구와 단위·범위를 만족하는지
  확인한 뒤 `\textbf{최종 답: }`으로 끝낸다.
- 좌표평면, 함수 그래프, 기하 도형, 확률수형도, 벤다이어그램,
  표본공간처럼 시각화가 풀이 이해에 실질적으로 도움이 되면 해설에도
  LaTeX TikZ로 직접 그린다. 그림 없이 핵심 관계가 불명확한 문항은
  텍스트만으로 끝내지 않는다.
- 해설용 그림은 해당 단계 바로 아래에 두고 `\linewidth` 안에
  들어오게 하며, 축·눈금·점·길이·각도·사건 이름 등 풀이에 사용한
  표기를 빠짐없이 넣는다. 장식용 그림은 추가하지 않는다.
- 원문과 답의 문항 수가 모두 일치해야 한다.

### 빌드와 DOCX

- Tectonic 또는 XeLaTeX로 문제지·해설지 PDF를 각각 컴파일한다.
- DOCX는 PDF 페이지를 A4 전면 이미지로 넣는 fidelity-first 포맷이다.
  다음 명령으로 만든다.

```bash
uv run \
  scripts/pdf_to_docx.py \
  <input.pdf> <output.docx>
```

### 검증

1. LaTeX 로그의 `Overfull`·`Underfull` 경고가 0인지 확인한다.
2. 문제지의 문항 수, 해설지의 해설 수·최종 답 수를 원본과 대조한다.
3. 문제지 전 페이지에서 풀이 공간이 보이고 답이 노출되지 않는지 본다.
4. LaTeX 소스에서 A4·글자 크기·여백·단 사이·구분선 값이 고정
   포맷과 정확히 일치하고, 렌더된 문제지와 해설지 전 페이지에
   동일하게 적용됐는지 확인한다.
5. 문제지 전 페이지가 고정 포맷의 2단이고, 긴 문항과 풀이 공간이
   지정된 `계속` 규칙으로만 이어지는지 확인한다.
6. 해설지 첫 페이지가 표 없는 2단 빠른정답이고, 이후 마지막
   해설까지 모든 페이지가 2단인지 확인한다.
7. 모든 해설에 난이도에 맞는 번호 단계가 있고, 심화 문항에서 핵심
   추론이나 경우 분류가 생략되지 않았는지 확인한다.
8. 해설에서 별도 시각화가 필요한 문항에는 텍스트 설명이나 원본
   크롭을 대신한 TikZ 그림이 있으며, 그림의 표기와 본문 계산이
   서로 일치하는지 확인한다. 원문 전사용 복잡 그림의 무손실 크롭은
   이 항목의 대상이 아니다.
9. 한 단보다 긴 해설은 단계 경계에서만 나뉘고 다음 단의 `계속`
   머리글 아래에서 이어지는지 확인한다.
10. PDF 전 페이지를 새로 렌더링해 머리말 잘림, 겹침, 누락, 한글 어절
   분리, 수식·표·그림 손상을 시각 검수한다.
11. DOCX를 LibreOffice에서 실제로 열어 PDF로 재변환한다. 페이지 수가
   원본 PDF와 같고 모든 페이지 이미지가 정상적으로 렌더되는지 확인한다.
12. 최종 경로에는 네 산출물만 남긴다.

검증 실패는 원인을 수정한 뒤 PDF부터 다시 생성한다. DOCX만 따로
손봐서 PDF와 내용이 달라지게 하지 않는다.
